import { FilterTemaPipe } from './filter-tema.pipe';

describe('FilterTemaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTemaPipe();
    expect(pipe).toBeTruthy();
  });
});
