import { FilterKategorijaPipe } from './filter-kategorija.pipe';

describe('FilterKategorijaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKategorijaPipe();
    expect(pipe).toBeTruthy();
  });
});
