import { FilterKomentaraPipe } from './filter-komentara.pipe';

describe('FilterKomentaraPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKomentaraPipe();
    expect(pipe).toBeTruthy();
  });
});
